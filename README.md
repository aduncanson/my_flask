# This is README.md for my flask application

Only Alice, Bob, Chris and Debbie have personalised pages.

The root page will indicate which users have their own pages and any other paths will return an error.